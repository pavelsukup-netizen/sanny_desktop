{
  "name": "sanny-desktop",
  "version": "0.4.0",
  "description": "Local Windows AI assistant with avatar, dual Ollama models, web search, operator tools and XTTS voice cloning.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check main.js && node --check preload.js"
  },
  "dependencies": {
    "electron-store": "^8.2.0"
  },
  "devDependencies": {
    "electron": "^39.2.7"
  }
}
